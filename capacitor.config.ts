import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.angulardemoapp',
  appName: 'angularDemoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
